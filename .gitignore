# ── OS / editor ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.iml
.fleet/

# ── Secrets / env ───────────────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.p8
*.p12
*.keystore
*.jks
keystore.properties
google-services.json
GoogleService-Info.plist

# ── Logs / temp ─────────────────────────────────────────────────────
*.log
*.pid
server.log
server.err.log
server.pid
*.tmp
*.bak

# ── Node / Expo ─────────────────────────────────────────────────────
node_modules/
.npm/
.expo/
.expo-shared/
.metro-cache/
.web-build/
dist-web/
coverage/
*.tgz
package-lock.json.bak

# ── Backend (SQLite + temp DB files) ────────────────────────────────
server/myhealth.db
server/myhealth.db-journal
server/myhealth.db-shm
server/myhealth.db-wal
server/test-*.db*

# ── Apple ───────────────────────────────────────────────────────────
xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/
DerivedData/
build/
*.xcarchive/
*.ipa
*.dSYM.zip
*.dSYM
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/

# Generated by xcodegen — keep project.yml as source of truth
*.xcodeproj
!ios/FitFusion.xcodeproj/.gitkeep
ios/FitFusion.xcodeproj/
watch/HealthAppWatch.xcodeproj/

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# ── Android ─────────────────────────────────────────────────────────
android/.gradle/
android/build/
android/captures/
android/app/build/
android/core/build/
android/wear/build/
android/local.properties
*.apk
*.aab
android/.kotlin/
android/.cxx/
android/.idea/
android/app/release/
android/wear/release/
android/key.properties

# Gradle wrapper jar — keep wrapper script + properties so users can bootstrap
android/gradle/wrapper/gradle-wrapper.jar
!android/gradle/wrapper/gradle-wrapper.properties
!android/gradlew
!android/gradlew.bat

# ── React Native / Expo extras ──────────────────────────────────────
mobile/.expo/
mobile/web-build/
mobile/.expo-shared/
mobile/ios/Pods/
mobile/ios/build/
mobile/android/app/build/
mobile/android/.gradle/
mobile/android/build/

# ── CI / scratch ────────────────────────────────────────────────────
distribution/staging-*/
.scratch/

# ── Misc binary / generated ─────────────────────────────────────────
*.zip
!distribution/whatsnew/**/*.zip
*.tar.gz
*.7z
*.so
*.dylib
*.dll

# === added-by-productionize-plan: keep desktop/build assets ===
!desktop/build/
!desktop/build/**

# ── Health Connect / HealthKit ──────────────────────────────────────
# Generated permission manifests / cache & exported PHI dumps must never
# land in the repo. Schemas under shared/schemas/ are intentionally tracked.
android/app/src/main/assets/health-connect-permissions.cache
android/wear/src/main/assets/health-connect-permissions.cache
*.healthkit.export
*.health-connect.export
*.phi.json
*.phi.csv
*.phi.sqlite
*.phi.sqlite-journal
phi-export/
audit-log-export/

# ── Signing secrets ─────────────────────────────────────────────────
keystore.properties
*.jks
*.keystore
ios/fastlane/report.xml
ios/fastlane/README.md

# ── Temporary test artifacts ────────────────────────────────────────
*.png
android_ui*.xml
*_test_results.txt
*_deep_test_results.txt
test_*.sh
test_*.py
android_screens/
android_screens_deep/
android_verification/
ios_screens/
ios_screens_deep/
ios_screenshots/

# ── SQLite WAL/SHM ──────────────────────────────────────────────────
*.db-shm
*.db-wal
